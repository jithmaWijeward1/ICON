% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{train_test_split_cells}
\alias{train_test_split_cells}
\title{Split Cells into Training and Testing Sets}
\usage{
train_test_split_cells(
  seurat_obj,
  column_name = "cell_type",
  train_proportion = 0.7,
  seed = 241,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object with cell type assignments.}

\item{column_name}{Name of the metadata column containing cell types
(default: \code{"cell_type"}).}

\item{train_proportion}{Proportion of cells to use for training
(default: \code{0.7}).}

\item{seed}{Random seed for reproducibility (default: \code{241}).}

\item{verbose}{Print progress messages (default: \code{TRUE}).}
}
\value{
A list containing:
  \describe{
    \item{train_data}{Seurat object with training cells.}
    \item{test_data}{Seurat object with testing cells.}
    \item{train_cells}{Vector of training cell names.}
    \item{test_cells}{Vector of testing cell names.}
    \item{distribution}{List with train and test cell type distributions.}
  }
}
\description{
Splits cells into training and testing sets, stratified by cell type. The
split is performed separately for each cell type to ensure balanced
representation.
}
\examples{
\dontrun{
split_result <- train_test_split_cells(seurat_obj, seed = 241)
train_data <- split_result$train_data
test_data <- split_result$test_data
}
}
