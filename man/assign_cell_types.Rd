% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{assign_cell_types}
\alias{assign_cell_types}
\title{Assign Cell Types to Data}
\usage{
assign_cell_types(
  seurat_obj,
  annotation_file = NULL,
  annotation_df = NULL,
  barcode_col = "cell",
  cell_type_col = "donor_id",
  column_name = "cell_type",
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{annotation_file}{Path to a CSV or TSV file containing cell type
annotations (optional if \code{annotation_df} is provided).}

\item{annotation_df}{Data frame containing cell type annotations (optional if
\code{annotation_file} is provided).}

\item{barcode_col}{Name of the column containing cell barcodes in the
annotation file (default: \code{"cell"}).}

\item{cell_type_col}{Name of the column containing cell types in the
annotation file (default: \code{"donor_id"}).}

\item{column_name}{Name of the metadata column to store cell types
(default: \code{"cell_type"}).}

\item{verbose}{Print progress messages (default: \code{TRUE}).}
}
\value{
A Seurat object with cell type assignments in metadata.
}
\description{
Assigns cell types by loading annotations from a file and merging with Seurat
object metadata.
}
\examples{
\dontrun{
# Using file path
seurat_obj <- assign_cell_types(seurat_obj,
                                annotation_file = "path/to/donor_ids.tsv")

# Using data frame
annotations <- read.csv("donor_ids.tsv", sep = "\t", header = TRUE)
seurat_obj <- assign_cell_types(seurat_obj, annotation_df = annotations)
}
}
