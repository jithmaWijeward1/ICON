% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{train_random_forest}
\alias{train_random_forest}
\title{Train Random Forest Model}
\usage{
train_random_forest(
  train_expr,
  train_labels,
  ntree = 100,
  importance = TRUE,
  seed = 241,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{train_expr}{Expression matrix for the training set (features as rows,
cells as columns).}

\item{train_labels}{Cell type labels for the training set (factor or
character vector).}

\item{ntree}{Number of trees to grow (default: \code{100}).}

\item{importance}{Logical, whether to calculate variable importance
(default: \code{TRUE}).}

\item{seed}{Random seed for reproducibility (default: \code{241}).}

\item{verbose}{Print progress messages (default: \code{TRUE}).}

\item{...}{Additional arguments passed to \code{randomForest::randomForest()}.}
}
\value{
A list containing:
  \describe{
    \item{model}{Trained Random Forest model.}
    \item{training_time}{Training time in seconds.}
  }
}
\description{
Trains a Random Forest model for cell type classification using expression
data.
}
\examples{
\dontrun{
rf_result <- train_random_forest(train_expr, train_labels, ntree = 100)
rf_model <- rf_result$model
}
}
