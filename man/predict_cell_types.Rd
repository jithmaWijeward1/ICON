% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{predict_cell_types}
\alias{predict_cell_types}
\title{Predict Cell Types Using Random Forest Model}
\usage{
predict_cell_types(
  rf_model,
  test_expr,
  test_data = NULL,
  column_name = "predicted_cell_type",
  return_probs = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{rf_model}{Trained Random Forest model (from \code{train_random_forest}).}

\item{test_expr}{Expression matrix for the test set (features as rows,
cells as columns).}

\item{test_data}{Seurat object for the test set (optional, for storing
predictions).}

\item{column_name}{Name of the metadata column to store predictions
(default: \code{"predicted_cell_type"}).}

\item{return_probs}{Logical, whether to return class probabilities
(default: \code{FALSE}).}

\item{verbose}{Print progress messages (default: \code{TRUE}).}
}
\value{
A list containing:
  \describe{
    \item{predicted_labels}{Predicted cell type labels (factor).}
    \item{predicted_probs}{Class probabilities matrix (if
      \code{return_probs = TRUE}).}
    \item{test_data}{Seurat object with predictions in metadata (if
      \code{test_data} provided).}
  }
}
\description{
Predicts cell types for test data using a trained Random Forest model.
Optionally stores predictions in Seurat object metadata.
}
\examples{
\dontrun{
predictions <- predict_cell_types(rf_model, test_expr, test_data)
predicted_labels <- predictions$predicted_labels
}
}
