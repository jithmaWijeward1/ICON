% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{Final_prediction_UMAP}
\alias{Final_prediction_UMAP}
\title{Generate UMAP Plot of Final Predictions}
\usage{
Final_prediction_UMAP(
  base_seurat,
  global_overlap = NULL,
  subset_overlap = NULL,
  subset_non_overlap_isoforms = NULL,
  global_pred_col = "predicted_global",
  subset_pred_col = "predicted_subset",
  subset_non_overlap_pred_col = "Subset_isoform_predicted_cell_type",
  reduction = "umap",
  dims = 1:10,
  cols = NULL,
  label = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{base_seurat}{A Seurat object to use as the base for the UMAP. Should
contain all test cells.}

\item{global_overlap}{Seurat object with global overlapping cells (optional).}

\item{subset_overlap}{Seurat object with subset overlapping cells (optional).}

\item{subset_non_overlap_isoforms}{Seurat object with subset non-overlapping
isoform cells (optional).}

\item{global_pred_col}{Column name for global predictions
(default: \code{"predicted_global"}).}

\item{subset_pred_col}{Column name for subset predictions
(default: \code{"predicted_subset"}).}

\item{subset_non_overlap_pred_col}{Column name for subset non-overlap
predictions
(default: \code{"Subset_isoform_predicted_cell_type"}).}

\item{reduction}{Dimensionality reduction to use for the plot
(default: \code{"umap"}).}

\item{dims}{PCA dimensions to use if UMAP needs to be computed
(default: \code{1:10}).}

\item{cols}{Optional named character vector of colours for cell types.}

\item{label}{Logical, whether to label clusters on the plot
(default: \code{TRUE}).}

\item{verbose}{Print progress messages (default: \code{TRUE}).}
}
\value{
A list containing:
  \describe{
    \item{seurat_object}{The base Seurat object with predictions added.}
    \item{umap_plot}{A ggplot2 UMAP plot coloured by predicted cell type.}
  }
}
\description{
Combines predictions from multiple sources (global overlap, subset overlap,
subset non-overlap) into a base Seurat object and generates a UMAP plot
coloured by predicted cell type.
}
\examples{
\dontrun{
umap_result <- Final_prediction_UMAP(isoform_seurat_obj,
                                     global_overlap = global_overlap,
                                     subset_overlap = subset_overlap,
                                     subset_non_overlap_isoforms = subset_non_overlap_isoforms)
print(umap_result$umap_plot)
}
}
