% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{extract_expression_data}
\alias{extract_expression_data}
\title{Extract Expression Data and Labels}
\usage{
extract_expression_data(
  train_data,
  test_data,
  column_name = "cell_type",
  features = NULL,
  find_variable_features = TRUE,
  nfeatures = 500,
  selection_method = "vst",
  use_variable_features = TRUE,
  layer = "data",
  verbose = TRUE
)
}
\arguments{
\item{train_data}{Seurat object for the training set.}

\item{test_data}{Seurat object for the testing set.}

\item{column_name}{Name of the metadata column containing cell types
(default: \code{"cell_type"}).}

\item{features}{Optional character vector of specific features to extract.
If provided, overrides \code{find_variable_features}.}

\item{find_variable_features}{Logical, whether to find variable features in
training set (default: \code{TRUE}).}

\item{nfeatures}{Number of variable features to select (default: \code{500}).}

\item{selection_method}{Method for selecting variable features
(default: \code{"vst"}).}

\item{use_variable_features}{Logical, whether to extract only variable
features (default: \code{TRUE}).}

\item{layer}{Layer to extract from (default: \code{"data"} for normalized
data).}

\item{verbose}{Print progress messages (default: \code{TRUE}).}
}
\value{
A list containing:
  \describe{
    \item{train_expr}{Expression matrix for training set (data frame).}
    \item{test_expr}{Expression matrix for testing set (data frame).}
    \item{train_labels}{Cell type labels for training set (factor).}
    \item{test_labels}{Cell type labels for testing set (factor).}
    \item{features_used}{Vector of feature names used.}
  }
}
\description{
Extracts normalized expression matrices and cell type labels for training
and testing sets. Optionally finds variable features in the training set.
}
\examples{
\dontrun{
expr_data <- extract_expression_data(train_data, test_data, nfeatures = 500)
train_expr <- expr_data$train_expr
train_labels <- expr_data$train_labels
}
}
